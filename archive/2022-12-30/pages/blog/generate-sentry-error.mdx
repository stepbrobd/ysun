import Meta from "../../components/meta";

<Meta
  title="Generate Sentry Error - Blog - Yifei Sun"
  description="Testing out Sentry.io's error monitoring integration."
  image="/assets/image/toc.webp"
  keywords="Yifei Sun, sentry.io, test, monitoring, error, error handling, personal site, portfolio"
  slug="/blog/generate-sentry-error"
/>

# Generate Sentry Error

This page is intended for testing out Sentry.io's error monitoring integration with multiple different types of errors.
Please reload after if you see any error message on this page.

## Throw an error based on `onClick` event

You need to open the console to see the error message (disabled).

<div className="flex items-center justify-center">
  <button
    type="button"
    className="inline-flex items-center rounded-md bg-[#E60000] px-6 text-base text-white hover:bg-[#F33]"
    onClick={() => {
      {
        /*throw new Error("Error");*/
      }
    }}
  >
    Throw Error
  </button>
</div>

```mdx
<button
  type="button"
  className="inline-flex items-center rounded-md bg-[#E60000] px-6 text-base text-white hover:bg-[#F33]"
  onClick={() => {
    throw new Error("Error");
  }}
>
  Throw Error
</button>
```
